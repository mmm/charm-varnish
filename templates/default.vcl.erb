# Generated by juju

<% current_backends.each do |server| %>
backend <%= server[:name] %> {
  .host = "<%= server[:address] %>";
  .port = "<%= server[:port] %>";
}
<% end %>

director baz round-robin {
  <% current_backends.each do |server| %>
  {
    .backend = <%= server[:name] %>;
  }
  <% end %>
}

sub vcl_recv {
  set req.backend = baz;
}

