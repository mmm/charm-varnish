# Generated by juju

sub vcl_recv {
  set req.backend = baz;
}

director baz round-robin {
  <% @backends.each do |server| %>
  {
    .backend = "<%= server[:name] %>";
  }
  <% end %>
}

<% @backends.each do |server| %>
backend foo {
  .host = "<%= server[:host] %>";
  .port = "<%= server[:port] %>";
}
<% end %>
